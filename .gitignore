# dependencies
/node_modules
/.pnp
.pnp.*

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
Thumbs.db
desktop.ini
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env files (opt in the example only)
.env*
!.env.local.example

# Razorpay's dashboard offers a "download as CSV" option when generating
# API keys — never let one of those land in the repo.
rzp-key*.csv

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# Editor/tooling
.vscode/
.idea/

# Claude Code — personal local settings, not shared (unlike .mcp.json,
# which IS committed so teammates get the same project MCP servers)
.claude/settings.local.json

# Agent skill caches — auto-fetched reference docs for AI coding tools,
# not application code
.agents/
skills-lock.json
